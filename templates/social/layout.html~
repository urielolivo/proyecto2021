<!DOCTYPE html>
{% load static %}
<html>
<head>
	<title>Django Social</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
        <link href='http://fonts.googleapis.com/css?family=Varela+Round' rel='stylesheet' type='text/css'>
	<link rel="stylesheet" type="text/css" href="{% static 'normalize.css' %}">
        <link rel="stylesheet" type="text/css" href="{% static 'style.css' %}">
</head>
<body>

	<header class="main-head">
		<nav class="main">
			<h1 id="logo">LLavero Ruso de Contraseñas </h1>
			<ul>
                                {%if user.is_authenticated %}
                                <li><a href="{% url 'logout' %}">Logout</a></li>
                                
				
                                {% else %}
                                
                                <li><h1 class="nuevoPassword"><a href="{% url 'registro' %}">Registrarse</a> </h1></li>
                                <li><a href="{% url 'login' %}">Login</a></li>
                                {% endif %}
				
				
			</ul>
		</nav>
	</header>

	{% if messages %}
            {% for message in messages %}
            <div class="notification {{ message.tags }}">
                 {{ message }}
            </div>
            {% endfor %}
        {% endif %}
	
	<div class="main">
		
		<nav class="second-nav">
			
                        {% if request.user.is_authenticated %}
                        <a class="profile" href="{%url 'perfil' %}">Mi Perfil</a>
                        <a class="profile" href="{% url 'password' %}">Registrar nuevo Password</a>
                        {% endif %}
			
		</nav>


		{% block content %}

		{% endblock content %}



	</div>

	<footer>
		
	</footer>

</body>
</html>
